# Plan 3b: groups.html UI（作成/参加/退会/招待）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development。チェックボックスで進捗管理。

**Goal:** ユーザーが画面からグループを作成・一覧・招待(リンク/QR)・参加(同意)・退会できる `groups.html` を作る。Plan2/3a で dev 実証済みの Worker エンドポイント(`/group-create //group-join //group-leave //group-pool-refresh`)を叩き、groups は Firestore SDK でメンバー読取する。

**Architecture:** 純ヘルパ(`js/group-client.js`: 招待URL生成/ slug 解析/ Workerベース解決)を TDD。その上に Worker/Firestore 呼び出しラッパ(createGroup/joinGroup/leaveGroup/refreshPool/listMyGroups)を載せ、`groups.html`＋`js/groups-app.js` が UI 配線。設定ページから導線。招待URL = `<base>/groups.html?group=gr-XXXXXX`、開くと同意画面→参加。

**Tech Stack:** Vanilla ESM JS、`node --test`。既存パターン流用: `settings.html` の紹介セクション(招待URL表示+QR `renderQrSvg`/`downloadQrPng`+コピー)、`js/qr-code.js`、`enforceAccess`(access-control.js)、`firebase-init.js`(auth/db)、`storage.js`(getMyUserId)。worktree `~/work/taxi-group-sharing`(branch feat/group-anon-sharing, sw v252)。

**前提（確認済み）:**
- dev Worker = `https://cabis-billing-dev.haqei64384.workers.dev`、本番 = `https://cabis-billing.haqei64384.workers.dev`。dev/prod 判定は `location.hostname/pathname.includes('-dev') || localhost`。
- エンドポイント: `POST /group-create {name, requireContributionToView?, minViewContribution?}`→`{ok,groupId,inviteSlug}` / `POST /group-join {slug}`→`{ok,status,groupId}` / `POST /group-leave {groupId}`→`{ok,status}` / `POST /group-pool-refresh {groupId,force?}`→`{ok,status,...}`。全て `Authorization: Bearer <Firebase IDトークン>` 必須。
- groups read ルール: メンバーのみ。`memberUserIds array-contains 自分のuserId` クエリは各doc でルールを満たすため許可される。
- tests/sw-precache-imports.test.js があるため、groups.html が静的 import する `./js/*.js` は **全て sw.js STATIC_FILES に登録必須**。

---

### Task 1: 純ヘルパ — buildGroupInviteUrl / parseGroupSlug / resolveWorkerBase

**Files:** Create `js/group-client.js` / Test `tests/group-client.test.js`

- [ ] **Step 1: 失敗テスト**

```js
import { test } from 'node:test';
import assert from 'node:assert';
import { buildGroupInviteUrl, parseGroupSlug, resolveWorkerBase } from '../js/group-client.js';

test('buildGroupInviteUrl: base + /groups.html?group=slug', () => {
  assert.equal(buildGroupInviteUrl('gr-abc123', 'https://app.taxicabis.com'),
    'https://app.taxicabis.com/groups.html?group=gr-abc123');
  assert.equal(buildGroupInviteUrl('gr-x', 'https://app.taxicabis.com/'), // 末尾スラッシュ吸収
    'https://app.taxicabis.com/groups.html?group=gr-x');
});

test('parseGroupSlug: ?group= の gr- slug だけ受理', () => {
  const ok = new URLSearchParams('group=gr-abc123');
  assert.equal(parseGroupSlug(ok), 'gr-abc123');
  assert.equal(parseGroupSlug(new URLSearchParams('group=co-xxxxxx')), null); // gr- 以外は不可
  assert.equal(parseGroupSlug(new URLSearchParams('group=<script>')), null); // 不正
  assert.equal(parseGroupSlug(new URLSearchParams('')), null);
});

test('resolveWorkerBase: dev/prod 判定', () => {
  assert.equal(resolveWorkerBase({ hostname: 'app.taxicabis.com', pathname: '/groups.html' }),
    'https://cabis-billing.haqei64384.workers.dev');
  assert.equal(resolveWorkerBase({ hostname: 'hidenaka.github.io', pathname: '/-taxi-daily-report-dev/groups.html' }),
    'https://cabis-billing-dev.haqei64384.workers.dev');
  assert.equal(resolveWorkerBase({ hostname: 'localhost', pathname: '/groups.html' }),
    'https://cabis-billing-dev.haqei64384.workers.dev');
});
```

- [ ] **Step 2: 失敗確認** `cd ~/work/taxi-group-sharing && node --test tests/group-client.test.js` → FAIL

- [ ] **Step 3: 実装**

```js
// グループUI用クライアント。純ヘルパ + Worker/Firestore 呼び出しラッパ。
// 招待URL生成・slug解析・Workerベース解決は純粋（テスト可能）。
const SLUG_RE = /^gr-[a-z0-9]{4,12}$/;

// 招待URL = <base>/groups.html?group=<slug>
export function buildGroupInviteUrl(slug, baseUrl) {
  const base = String(baseUrl || '').replace(/\/+$/, '');
  return `${base}/groups.html?group=${encodeURIComponent(slug)}`;
}

// ?group=<slug> から gr- 形式の slug だけ受理（不正は null）。
export function parseGroupSlug(searchParams) {
  const raw = searchParams && searchParams.get ? searchParams.get('group') : null;
  return raw && SLUG_RE.test(raw) ? raw : null;
}

// Worker のベースURLを dev/prod 判定で返す。loc = {hostname, pathname}（既定 location）。
export function resolveWorkerBase(loc = (typeof location !== 'undefined' ? location : {})) {
  const host = loc.hostname || '';
  const path = loc.pathname || '';
  const isDev = host.includes('-dev') || path.includes('-dev') || host === 'localhost' || host === '127.0.0.1';
  return isDev
    ? 'https://cabis-billing-dev.haqei64384.workers.dev'
    : 'https://cabis-billing.haqei64384.workers.dev';
}
```

- [ ] **Step 4: 成功確認** → PASS（3 tests）
- [ ] **Step 5: Commit** `git add js/group-client.js tests/group-client.test.js && git commit -m "feat(group-client): 招待URL/ slug解析/ Workerベース解決(純ヘルパ)"`

---

### Task 2: Worker/Firestore ラッパ — callWorker / create/join/leave/refresh / listMyGroups

**Files:** Modify `js/group-client.js` / Test 同上

- [ ] **Step 1: 失敗テスト**（先頭importに callWorker 追加。fetch 注入でテスト）

```js
test('callWorker: Bearerトークン付きPOSTしてjsonを返す', async () => {
  let seen = null;
  const fakeFetch = async (url, opts) => { seen = { url, opts }; return { ok: true, status: 200, json: async () => ({ ok: true, x: 1 }) }; };
  const r = await callWorker('https://w.example', '/group-create', { name: 'g' }, 'TOK', fakeFetch);
  assert.deepEqual(r, { ok: true, x: 1 });
  assert.equal(seen.url, 'https://w.example/group-create');
  assert.equal(seen.opts.method, 'POST');
  assert.equal(seen.opts.headers.Authorization, 'Bearer TOK');
  assert.equal(JSON.parse(seen.opts.body).name, 'g');
});

test('callWorker: 非okでも json を返す（呼び側でstatus判定）', async () => {
  const fakeFetch = async () => ({ ok: false, status: 403, json: async () => ({ error: 'not-a-member' }) });
  const r = await callWorker('https://w', '/group-leave', {}, 'T', fakeFetch);
  assert.deepEqual(r, { error: 'not-a-member' });
});
```

- [ ] **Step 2: 失敗確認** → FAIL（callWorker 無し）

- [ ] **Step 3: 実装**（追記。callWorker は純粋に近い。create/join/leave/refresh/listMyGroups は実 SDK 依存のため getIdToken/db を引数で受ける形にして薄く保つ）

```js
// Worker への認証付きPOST（fetch 注入可・テスト用）。json を返す（status は呼び側で見る）。
export async function callWorker(workerBase, path, body, idToken, fetchImpl = fetch) {
  const res = await fetchImpl(workerBase + path, {
    method: 'POST',
    headers: { Authorization: 'Bearer ' + idToken, 'Content-Type': 'application/json' },
    body: JSON.stringify(body || {}),
  });
  return res.json();
}

// 以下は実行時に auth/db を渡して使う薄いラッパ（ブラウザ専用・ユニットテスト対象外）。
//   ctx = { idToken, workerBase }
export function createGroup(ctx, { name, requireContributionToView, minViewContribution }) {
  return callWorker(ctx.workerBase, '/group-create', { name, requireContributionToView, minViewContribution }, ctx.idToken);
}
export function joinGroup(ctx, slug) {
  return callWorker(ctx.workerBase, '/group-join', { slug }, ctx.idToken);
}
export function leaveGroup(ctx, groupId) {
  return callWorker(ctx.workerBase, '/group-leave', { groupId }, ctx.idToken);
}
export function refreshPool(ctx, groupId, force = false) {
  return callWorker(ctx.workerBase, '/group-pool-refresh', { groupId, force }, ctx.idToken);
}

// 自分が所属する groups を Firestore から取得（memberUserIds array-contains 自分のuserId）。
//   fs = firestore モジュール, db, myUserId
export async function listMyGroups(fs, db, myUserId) {
  const q = fs.query(fs.collection(db, 'groups'), fs.where('memberUserIds', 'array-contains', myUserId));
  const snap = await fs.getDocs(q);
  return snap.docs.map((d) => ({ groupId: d.id, ...d.data() }));
}
```

- [ ] **Step 4: 成功確認** → PASS（5 tests）
- [ ] **Step 5: 全スイート** `node --test tests/*.test.js 2>&1 | tail -5` → 全PASS
- [ ] **Step 6: Commit** `git add -A && git commit -m "feat(group-client): callWorker/create/join/leave/refresh/listMyGroups ラッパ"`

---

### Task 3: groups.html ＋ js/groups-app.js（UI）

**Files:** Create `groups.html`, Create `js/groups-app.js`

> 既存 `settings.html`（特に紹介セクション 250-265, 380-445 の招待URL+QR+コピー）と他ページのヘッダ/`navHost`/`enforceAccess`/`renderBottomNav`/`renderLegalFooter` パターンに**厳密に倣う**こと。

- [ ] **Step 1: groups.html を作成**（他ページと同じ骨格）

要素:
- `<head>`: 既存ページ同様（style.css, manifest, viewport, title「Cabis ｜ グループ」）。
- 本文カード:
  1. **新規作成**: グループ名入力 + 「閲覧条件」選択(ラジオ: 「参加すれば見られる」=requireContributionToView:false / 「自分も提供してから見られる」=true) + 「作成」ボタン。
  2. **参加（招待で来た時）**: `?group=<slug>` がある場合に同意カードを上部表示:「このグループに参加すると、あなたの過去含む営業情報が匿名でこのグループのプールに入ります（個人や合計は出ません）」+「参加する」ボタン。
  3. **自分のグループ一覧**: 各グループ = 名前 / 人数(memberUserIds.length)「N人」 / 招待リンク表示+「📋コピー」+「📱QR」(renderQrSvg/downloadQrPng) / 「退会」ボタン。
  4. `<div id="navHost">` + 下部ナビ(renderBottomNav('settings') 等、適切なキー)・legal footer。
- `<script type="module">`（または js/groups-app.js を import）。

- [ ] **Step 2: js/groups-app.js を作成**（ページ配線）

import: `enforceAccess`(access-control), `renderBottomNav`/`renderLegalFooter`(app/legal-footer), `auth, db`(firebase-init), `getMyUserId`(storage), `renderQrSvg, downloadQrPng`(qr-code), `buildGroupInviteUrl, parseGroupSlug, resolveWorkerBase, createGroup, joinGroup, leaveGroup, listMyGroups`(group-client)。

ロジック:
- 冒頭 `await enforceAccess()`（他ページ同様、未契約はリダイレクト）。`navHost.innerHTML = renderBottomNav(...)`。
- `getIdToken()`: `await auth.currentUser.getIdToken()`。`ctx = { idToken, workerBase: resolveWorkerBase() }`。
- `firestore` モジュールは動的 import（`https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js`）して listMyGroups/QR表示に使う。
- 一覧描画: `const fs = await import(...firestore...); const groups = await listMyGroups(fs, db, getMyUserId());` → カード描画。招待URL = `buildGroupInviteUrl(g.inviteSlug, location.origin + basePath)`（dev はサブパスに注意。`new URL('.', location.href)` 等で base を作る or `location.origin + location.pathname.replace(/groups\.html$/,'')`）。
- 作成: createGroup(ctx, {...}) → ok なら一覧再描画 + その招待URLを表示。
- 参加: parseGroupSlug(new URLSearchParams(location.search)) があれば同意カード表示 → 「参加する」で joinGroup(ctx, slug) → ok/already なら一覧再描画 + URL から ?group= を消す(history.replaceState)。no-group は「無効な招待です」。
- 退会: leaveGroup(ctx, groupId) → 確認ダイアログ後、ok なら一覧から除去・再描画。
- コピー: `navigator.clipboard.writeText(url)`、QR: `renderQrSvg(url,{cellSize:5,margin:4})` を innerHTML / `downloadQrPng(url, 'group-invite.png', {cellSize:10,margin:4})`。
- エラーは画面にメッセージ表示（alert か status div）。

- [ ] **Step 3: 構文チェック** `node --check js/groups-app.js`（HTML内インラインなら groups.html はブラウザ確認）。`node --check js/group-client.js`。
- [ ] **Step 4: Commit** `git add groups.html js/groups-app.js && git commit -m "feat(groups-ui): groups.html 作成/一覧/招待QR/参加同意/退会"`

---

### Task 4: settings.html からの導線

**Files:** Modify `settings.html`

- [ ] **Step 1:** 設定ページに「👥 グループ（営業情報を仲間と匿名共有）」のリンクカードを1つ追加（既存の guide.html リンク 65行目パターンに倣う）: `<a href="groups.html" ...>`。
- [ ] **Step 2: Commit** `git add settings.html && git commit -m "feat(settings): groups.html への導線追加"`

---

### Task 5: SW 登録 + CACHE_NAME bump

**Files:** Modify `sw.js`

- [ ] **Step 1:** `STATIC_FILES` に `'./groups.html'`, `'./js/group-client.js'`, `'./js/groups-app.js'` を追加（js は js/ 群の並びに）。`CACHE_NAME` を `v252` → `v253` に bump。
- [ ] **Step 2: precache-imports テスト確認** `node --test tests/sw-precache-imports.test.js` → PASS（groups.html の静的 import が全て STATIC_FILES にある）。落ちたら不足ファイルを STATIC_FILES に足す。
- [ ] **Step 3:** `node --check sw.js`、全スイート `node --test tests/*.test.js 2>&1 | tail -5` → PASS。
- [ ] **Step 4: Commit** `git add sw.js && git commit -m "chore(sw): groups.html/group-client/groups-app を STATIC_FILES 追加・CACHE_NAME v253"`

---

### Task 6: dev ライブ検証（kimi-webbridge・実アカウント）

- [ ] dev へ反映（コードは GitHub Pages 配信なので dev push 要）: `!~/work/taxi-dev/dpush.sh ~/work/taxi-group-sharing`（または承認の上 worktree から dev へ push）。SW更新のため PWA 再起動案内。
- [ ] kimi-webbridge で dev の groups.html を開き（実アカウント admin）: グループ作成→一覧に出る/人数1→招待URL/QR表示→（別アカ無しなら）退会で消える、を画面操作で確認。コンソールエラー無し。
- [ ] 招待URL を ?group= で開く→同意カード→参加 の動線確認。

---

## このプラン完了後
画面からグループの作成/招待/参加/退会が可能に。Plan3c(日報オプトアウト)・Plan4(分析にプール表示)で「共有した情報を見る」体験が完成。

## Self-Review（記録）
- spec §7 UI（作成/招待/参加同意/退会/人数のみ/閲覧条件） → Task3。✓
- Worker仲介（ID Token） → callWorker/create/join/leave。✓
- 招待 `?group=gr-XXXXXX` → buildGroupInviteUrl/parseGroupSlug。✓
- listMyGroups は array-contains（ルール適合）。✓
- 新規ファイルは SW STATIC_FILES 登録＋bump（precache-imports テストで担保）。✓
- placeholder: groups-app の base URL 組み立ては dev サブパス対応を実装時に確認（`new URL('groups.html', location.href)` 等）。型整合: ctx={idToken,workerBase}・group {groupId,memberUserIds,inviteSlug,name,...} 一貫。
